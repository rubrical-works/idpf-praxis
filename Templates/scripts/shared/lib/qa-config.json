{
  "$schema": "./qa-config-schema.json",
  "keywords": [
    "manually verify",
    "manually validate",
    "manually confirm",
    "manually check",
    "manual test",
    "manual check",
    "manual smoke test",
    "manual smoke",
    "smoke test",
    "visual smoke",
    "visually confirm",
    "visual verification",
    "exploratory test",
    "click through",
    "walk through manually",
    "UX walkthrough",
    "QA:",
    "qa-required"
  ],
  "bodyTemplate": "## QA: {acDescription}\n\nParent: #{parentIssue} — {parentTitle}\n\n### Steps to Perform\n\n1. {steps}\n\n### Expected Result\n\n{expectedResult}",
  "subIssueCommand": "gh pmu sub create --parent $ISSUE --title \"QA: [AC description]\" --label qa-required -F .tmp-qa-body.md",
  "annotationFormat": "- [ ] {acText} → QA: #{subIssueNumber}",
  "closurePath": "Parent stays in_review until all QA sub-issues are closed and AC checked off."
}
